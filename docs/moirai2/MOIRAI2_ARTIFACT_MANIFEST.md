# Moirai 2.0 Artifact Manifest

Status: `PARTIALLY_VERIFIED`.

This initial isolated increment contains 52 Moirai-owned files before excluding
`MOIRAI2_SHA256SUMS` from its own checksum inventory. It includes:

- two exact-version runtime lane declarations;
- strict Pydantic request and response contract v2;
- dynamic game geometry, time mapping, token geometry, quantile validation, provenance,
  post-processing, license policy, subprocess runner, and adapter modules;
- focused tests;
- requirements, specification, architecture, data contract, provider protocol, package/model
  provenance, license policy, matrices, runbook, handoff, changelog, and evaluation placeholders.

Evaluation CSV files marked `EXECUTION_PENDING` are schema placeholders and are not accuracy
claims. `MOIRAI2_SHA256SUMS` covers every changed Moirai-owned file in this increment except
itself, using repository-relative paths.
