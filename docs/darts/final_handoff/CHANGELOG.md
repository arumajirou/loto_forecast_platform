# Changelog

## 2026-08-05 — final handoff package

- added deterministic package construction and verification;
- added exact 12-document handoff contract;
- added SHA-256, path-safety, ZIP-metadata, CRC, extraction, and tamper gates;
- added 10 focused package tests;
- created the downloadable final handoff ZIP.

## P12 — cross-library comparison

Added eight provider tracks, common fairness hashes, identical prediction-key requirements,
wrapper/base identity separation, algorithm deduplication, multi-seed aggregation, baseline
gates, provider failure retention, and champion decision contracts.

## P11 — persistence and GPU

Added process-boundary save/load, clean save, best/last checkpoint, load weights, CPU/CUDA
map-location, prediction replay, GPU PID/VRAM, and CPU-fallback rejection contracts.

## P10 — ensemble and conformal

Added naive mean parity, regression stacking isolation, conformal quantile and median parity,
interval coverage, interval width, and calibration/evaluation separation contracts.

## P9 — historical evaluation

Added historical forecasts, retrain schedules, backtest metric parity, residual parity, and
optimized/general-path parity.

## P5-P8 — model families

Added Local statistical, Regression, Torch, and Foundation model identity, argument,
capability, device, and failure-retention contracts.

## P1-P4 — foundation

Added isolated environments, provider protocol, runtime discovery, TimeSeries adapters, game
geometry, and initial fake-runtime execution contracts.
