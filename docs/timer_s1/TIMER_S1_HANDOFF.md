# Timer-S1 handoff

PR-B must begin from the latest main after PR-A is merged. Before loading the model, obtain and
independently verify every config, index, shard, and remote Python SHA-256 at one immutable Hub
revision. Generate a new isolated `uv.lock`, review all packages and licenses, and run offline.

Formal GPU certification requires requested/effective CUDA identity, no CPU fallback, external
provider PID visibility, one GPU UUID, process VRAM, and provider PID disappearance after exit.
