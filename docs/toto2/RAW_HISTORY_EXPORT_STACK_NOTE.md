# Stack note

This change is stacked on Draft PR #102 and must remain Draft until a real read-only PostgreSQL
export and independent verification pass on the target host. Use squash merge only after all parent
PRs and runtime gates are complete.
