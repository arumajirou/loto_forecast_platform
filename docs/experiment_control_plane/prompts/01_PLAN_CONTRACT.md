# Stage Prompt: plan-contract

Use `../IMPLEMENTATION_PROMPT.md` in full with:

```text
TARGET_STAGE=plan-contract
```

Additional stage boundary:

Implement only strict Plan/identity schemas, canonicalization, hashing, semantic validators, examples and focused tests. No persistence or execution.

Do not omit the fresh audit, safety, local gate, artifact, push or Draft PR sections from the authoritative prompt.
