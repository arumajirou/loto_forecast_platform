# Stage Prompt: evidence-adapters

Use `../IMPLEMENTATION_PROMPT.md` in full with:

```text
TARGET_STAGE=evidence-adapters
```

Additional stage boundary:

Integrate real PostgreSQL, MLflow, object storage and downstream handoff idempotently without bypassing canonical owners.

Do not omit the fresh audit, safety, local gate, artifact, push or Draft PR sections from the authoritative prompt.
