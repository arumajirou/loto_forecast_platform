# Lock status

`EXECUTION_PENDING / PACKAGE_REGISTRY_BLOCKED`

Generate `uv.lock` only in an environment that can resolve `darts[torch]==0.46.1`.
The future lock must record the exact PyTorch and Lightning resolution used for certification.
