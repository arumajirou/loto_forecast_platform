[project]
name = "loto-forecast-mcp-runtime"
version = "0.1.0"
description = "Isolated frozen runtime for the loopback-only TAJ-69 Forecast MCP server"
requires-python = ">=3.11,<3.13"
dependencies = [
  "mcp==2.0.0",
  "pydantic==2.13.4",
]

[tool.uv]
package = false
