[project]
name = "loto-timesfm-provider"
version = "0.1.0"
description = "Dedicated TimesFM provider environment for loto-forecast-platform"
requires-python = ">=3.11,<3.13"
dependencies = [
  "numpy>=1.26,<3",
  "pandas>=2.2",
  "torch>=2.3",
  "huggingface-hub>=0.24",
  "timesfm[torch]>=2.0.0,<3",
]

[tool.uv]
package = false
