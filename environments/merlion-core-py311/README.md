# Merlion core isolated environment

This project is intentionally separate from the root NumPy 2.x environment.

`uv.lock` is generated by `scripts/bootstrap_merlion_core_env.sh`, reviewed, and committed before
formal runtime certification. The bootstrap result alone is never a runtime PASS.
