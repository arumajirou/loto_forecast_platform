# Timer-S1 isolated Python 3.11 lane

This declaration is PR-A provenance only. It has no reviewed `uv.lock`, does not install
packages, does not download the checkpoint, and does not certify Python, CPU, CUDA, or
16 GiB offload compatibility. Generate and review a lock only in PR-B.
