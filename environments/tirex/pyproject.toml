[project]
name = "loto-tirex-provider"
version = "0.1.0"
description = "Isolated TiRex provider runtime for loto forecast platform validation"
requires-python = ">=3.11,<3.13"
dependencies = [
  "tirex-2>=0.1.1,<0.2",
  "huggingface-hub>=0.24",
  "numpy>=1.26",
  "pandas>=2.2",
  "torch>=2.3",
  "pyyaml>=6.0",
]
