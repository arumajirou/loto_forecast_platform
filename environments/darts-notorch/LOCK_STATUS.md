# Lock status

`EXECUTION_PENDING / PACKAGE_REGISTRY_BLOCKED`

Generate `uv.lock` only in an environment that can resolve `darts[notorch]==0.46.1`.
Do not fabricate or copy a lockfile from another extra.
