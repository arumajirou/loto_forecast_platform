[project]
name = "loto-timer-base-84m-provider"
version = "0.1.0"
description = "Isolated fail-closed Timer Base 84M provider contract lane"
requires-python = ">=3.10,<3.11"
dependencies = [
  "transformers==4.40.1",
  "huggingface-hub>=0.24,<1",
  "safetensors>=0.4,<1",
  "numpy>=1.26,<3",
  "pydantic>=2.10,<3",
]

# PR-A intentionally omits torch and uv.lock. PR-B must select and review an exact
# compatible torch version before any checkpoint load or inference is permitted.
