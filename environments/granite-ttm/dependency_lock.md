# Granite TTM Dependency Lock

No resolved lockfile was generated in this run because the dedicated environment was not installed.

Pinned input constraints are recorded in:

- `environments/granite-ttm/pyproject.toml`
- `environments/granite-ttm/environment_manifest.json`

Generate the lock after dependency approval:

```bash
uv lock --project environments/granite-ttm
```
