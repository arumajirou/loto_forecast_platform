# Plan

1. Add a fail-closed manifest parser and validator.
2. Add deterministic template and status report generation.
3. Add `loto3 revisions` CLI actions.
4. Test invalid mutable references, mismatches, duplicates, completeness and immutability.
5. Package with guarded installation and rollback.
