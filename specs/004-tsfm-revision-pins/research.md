# Research

A branch or tag is mutable and cannot establish experiment reproducibility. A shortened hash is ambiguous. The accepted contract is therefore a full 40-character SHA-1 or 64-character SHA-256 object identifier, tied to both the catalog `model_id` and exact `repo_id`.
