# Quickstart

Run `tools/setup_local.sh`, then `tools/verify_full_implementation.sh`. Aggregate a campaign with `uv run python scripts/analysis/aggregate_condition_contributions.py --input RESULTS.csv --output-dir OUT`.
